import { Layout } from '@/components/Layout/Layout';
import { LocationsPage } from '@/pages';

const Locations = () => {
    return (
        <Layout title='Страница фильтрации Локаций'>
            <LocationsPage/>
        </Layout>
    )
}

export default Locations