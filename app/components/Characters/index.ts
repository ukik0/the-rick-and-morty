import { CharacterCard } from './CharacterCard/CharacterCard';
import { CharacterStatus } from './CharacterStatus/CharacterStatus';
import { CharacterEpisodes } from './CharacterEpisodes/CharacterEpisodes';
import { CharacterList } from './CharacterList/CharacterList';
import { CharactersShowMore } from './CharactersShowMore/CharactersShowMore';

export const Characters = {
    Card: CharacterCard,
    Status: CharacterStatus,
    Episodes: CharacterEpisodes,
    List: CharacterList,
    Show: CharactersShowMore
}